import cse.utils.*;

public class swapping {
  public static void main(String[] args) {
    Integer x = 9;
    Integer y = 6;
    spec.println(x.toString() + y.toString());
    spec.swap(x, y);
    spec.println(x.toString() + y.toString());
    return;
  }
}
